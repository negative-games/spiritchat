name: SpiritChat
version: '${version}'
main: games.negative.spiritchat.SpiritChatPlugin
loader: games.negative.spiritchat.loader.SpiritChatPluginLoader
api-version: '1.21'
authors: ["ericlmao"]
dependencies:
  server:
    PlaceholderAPI:
      load: AFTER
      required: false
    LuckPerms:
      load: AFTER
      required: false
